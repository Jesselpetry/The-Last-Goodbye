# คู่มือการใช้งาน The Last Goodbye 📖

คู่มือนี้อธิบายการใช้งานระบบ The Last Goodbye สำหรับเจ้าของระบบ (Admin)

## 🏠 หน้าหลัก

### เข้าสู่ระบบ Admin
1. เปิด URL ของเว็บไซต์ เช่น `https://your-domain.com/admin`
2. ใส่ PIN (รหัสผ่าน Admin)
3. จะเข้าสู่หน้า Dashboard ของ Admin

---

## 📊 Dashboard

Dashboard แสดงภาพรวมของจดหมายทั้งหมด:

### สถานะจดหมาย
- **🔒 ยังไม่มีใครสแกน**: ไม่มีใครสแกน QR Code เลย
- **👀 สแกนแล้ว แต่ยังไม่เปิด**: มีคนสแกนแล้ว แต่ยังไม่ใส่รหัสถูกต้อง
- **✅ เปิดอ่านแล้ว**: มีคนใส่รหัสถูกและอ่านจดหมายแล้ว

---

## 👥 จัดการเพื่อน

### เพิ่มเพื่อนใหม่
1. คลิก "➕ เพิ่มเพื่อนใหม่"
2. กรอกข้อมูล:
   - **ชื่อเพื่อน**: ชื่อที่จะแสดงในจดหมาย เช่น "น้องมิ้นท์"
   - **Slug (URL)**: ส่วนท้ายของ URL เช่น `mint` จะได้ URL เป็น `your-domain.com/mint`
   - **รหัสผ่าน 4 หลัก**: รหัสที่เพื่อนต้องใส่เพื่อเปิดอ่าน
   - **Background Music**: เลือกเพลงประกอบที่จะเล่นเมื่อเปิดจดหมาย
   - **เนื้อหาจดหมาย**: ข้อความในจดหมาย

### 👁️ Live Preview (ตัวอย่างก่อนส่ง)
ในหน้า "เพิ่ม/แก้ไขเพื่อน" จะมีปุ่ม "👁️ Live Preview" อยู่มุมขวาบน
- คลิกเพื่อดูหน้าตาจดหมายจริงๆ ที่เพื่อนจะเห็น
- สามารถกดปุ่ม Mobile/Desktop เพื่อดูในขนาดหน้าจอต่างๆ ได้
- เนื้อหาจะอัปเดตทันทีที่คุณพิมพ์ในฟอร์ม
- **หมายเหตุ**: ในโหมด Preview ซองจดหมายจะเปิดให้อัตโนมัติและเพลงจะไม่เล่นอัตโนมัติเพื่อไม่ให้รบกวน

### แก้ไข/ลบเพื่อน
- คลิก "✏️ แก้ไข" เพื่อแก้ไขข้อมูล
- คลิก "🗑️ ลบ" เพื่อลบข้อมูลถาวร

---

## 💬 ข้อความตอบกลับ (Reply System)

เมื่อเพื่อนเปิดอ่านจดหมาย เพื่อนสามารถเขียนข้อความตอบกลับหาคุณได้ที่ท้ายจดหมาย

### การแจ้งเตือน
- จะมี 🔔 แจ้งเตือนสีแดงที่เมนู "ข้อความตอบกลับ" เมื่อมีข้อความใหม่ที่ยังไม่อ่าน

### การอ่านและจัดการ
1. คลิกที่เมนู "ข้อความตอบกลับ"
2. จะเห็นรายการข้อความทั้งหมด เรียงจากใหม่ไปเก่า
3. ข้อความที่ยังไม่อ่านจะมีขอบสีฟ้า
4. คลิกปุ่ม "✅ ทำเครื่องหมายว่าอ่านแล้ว" เพื่อลบการแจ้งเตือน
5. สามารถคลิก "ดูข้อมูลเพื่อน" เพื่อไปดูรายละเอียดเพื่อนคนนั้นได้

---

## 🕵️ Spy Logs / Analytics

หน้านี้แสดงข้อมูลการเข้าชมทั้งหมด
- **Browser**: Line, Instagram, Facebook, Chrome, Safari
- **IP Address**: ใช้ดูคร่าวๆ ว่าเป็นคนเดียวกันหรือไม่

---

## 🎴 การสร้าง QR Code

### วิธีสร้าง QR Code
1. เปิดเว็บ [qr-code-generator.com](https://www.qr-code-generator.com)
2. ใส่ URL ของเพื่อน เช่น `https://your-domain.com/mint`
3. ดาวน์โหลด QR Code

---

## ❓ คำถามที่พบบ่อย

### Q: เพื่อนส่งข้อความตอบกลับได้กี่ครั้ง?
A: ไม่จำกัด เพื่อนสามารถส่งได้หลายข้อความ และข้อความที่ส่งแล้วจะแสดงในหน้าจดหมายของเพื่อนด้วย

### Q: เปลี่ยนเพลง BGM ได้ที่ไหน?
A: ไฟล์เพลงต้องอยู่ในโฟลเดอร์ `public/bgm/` และต้องเพิ่มรายชื่อในไฟล์ `lib/bgm-config.ts` (ต้องแก้โค้ด)

---

## 📞 ติดต่อ Support

หากพบปัญหาหรือต้องการความช่วยเหลือ ติดต่อผู้พัฒนา

---

ขอให้สนุกกับการส่งความทรงจำให้เพื่อนๆ นะคะ/ครับ! 💕
